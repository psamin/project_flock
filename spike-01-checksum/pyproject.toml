[project]
name = "spike-01-checksum"
version = "0.1.0"
description = "Cross-engine canonical checksum spike: PostgreSQL vs CockroachDB"
requires-python = ">=3.11"
dependencies = [
    "psycopg[binary]>=3.2",
    "pytest>=8.0",
    "hypothesis>=6.100",
]

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["."]
